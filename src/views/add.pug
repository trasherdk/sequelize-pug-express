extends layout

block content
  h1 #{title}
  form(action='/add',method="POST")
    .form-group
      label(for='author') Author
      input#author.form-control(type='text',name='author')
    .form-group
      label(for='author') Title
      input#author.form-control(type='text',name='title')
    .form-group
      label(for='text') Text
      textarea#text.form-control(rows='3',name='text')
    button.btn.btn-primary(type='submit') Add Articles
